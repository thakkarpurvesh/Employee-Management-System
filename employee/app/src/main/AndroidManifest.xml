<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.purvesh.employee">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EmployeeNavigationActivity"
            android:label="@string/title_activity_employee_navigation"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".userloginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".forgotActivity" />
        <activity android:name=".GridViewActivity" />
        <activity
            android:name=".employeemanageActivity"
            android:label="@string/title_activity_employeemanage"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AdminloginActivity"
            android:label="@string/title_activity_adminlogin"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Project_manager_navActivity"
            android:label="@string/title_activity_project_manager_nav"
            android:theme="@style/AppTheme" />
    </application>

</manifest>